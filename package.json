{
  "name": "onclick",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @onclick/dashboard dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  }
}
